name: Update version-set

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  versionset:
    if: "!contains(github.event.head_commit.message, 'skip ci')"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout current code
        uses: actions/checkout@v3
        with:
          path: 'source'

      - name: Get values
        id: vars
        run: |
          echo GITHUB_REF_NAME: $GITHUB_REF_NAME
          [ -f source/.versionset ] || { echo ".versionset file missing"; exit 1; }
          echo "version_set_name=$(cat source/.versionset)" | tee $GITHUB_OUTPUT

      - name: Checkout version-set repo
        uses: actions/checkout@v3
        with:
          path: 'versionset'
          repository: geokrety/justatest2

      - name: Update Version-Set
        uses: fjogeleit/yaml-update-action@main
        with:
          valueFile: 'main.yml'
          changes: |
            {
              "${{ steps.vars.outputs.version_set_name }}.docker_tag": "${{ vars.GITHUB_REF_NAME }}",
              "${{ steps.vars.outputs.version_set_name }}.git_url": "${{ vars.GITHUB_SERVER_URL }}/{{ vars.GITHUB_REPOSITORY }}"
            }

          repository: geokrety/justatest2
          targetBranch: main

          token: ${{ secrets.GH_TOKEN }}
          workDir: versionset

          createPR: true
          masterBranchName: main
          branch: deployment/$GITHUB_REPOSITORY/$GITHUB_REF_NAME
          commitUserName: geokrety-bot
          commitUserEmail: geokrety-bot@users.noreply.github.com
          message: |
            feat: Update $GITHUB_REPOSITORY version to ${{ vars.GITHUB_REF_NAME }}
